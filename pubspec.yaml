name: finance_tracker
description: تطبيق تتبع الإيرادات والمصروفات
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  uuid: ^4.2.1
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  fl_chart: ^0.66.2
  flutter_slidable: ^3.0.1
  provider: ^6.1.2
  flutter_contacts: ^2.1.0
  path_provider: ^2.1.2
  file_picker: ^8.1.2
  google_sign_in: ^6.2.1
  googleapis: ^13.2.0
  googleapis_auth: ^1.6.0
  extension_google_sign_in_as_googleapis_auth: ^2.0.12
  firebase_core: ^3.3.0
  cloud_firestore: ^5.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
